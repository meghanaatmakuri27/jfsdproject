// components/Interviews.js
import React from "react";
import { Typography } from "@mui/material";

const Interviews = () => {
  return <Typography variant="h4">Interviews Page</Typography>;
};

export default Interviews;
