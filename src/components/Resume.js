// components/Resume.js
import React from "react";
import { Typography } from "@mui/material";

const Resume = () => {
  return <Typography variant="h4">Resume Page</Typography>;
};

export default Resume;
